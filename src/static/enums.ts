export enum NonLoyaltyPaymentMethods {
  upi = 'upi',
  netBanking = 'netBanking',
  creditCard = 'creditCard',
  debitCard = `debitCard`,
}

export enum UserTypes {
  customer = 'customer',
  merchant = 'merchant',
}
